# The sphinx configuration file sets this variable to true to indicate that
# it's running.  So modules can check this variable if they need to do
# something different when documentation is being generated (e.g., not load
# some settings).
is_running = False
