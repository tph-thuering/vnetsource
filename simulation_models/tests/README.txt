The package tests are written using Python's unittest library.
They must be run from the project's root directory:

  $ cd {PROJECT_ROOT}
  $ python -m unittest [-v] simulation_models.tests.model_id_tests

For help running the unittest module:

  $ python -m unittest -h